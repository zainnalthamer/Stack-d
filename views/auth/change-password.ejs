<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Change Password | STACKD</title>
  <link rel="stylesheet" href="/styles/style.css" />
</head>
<body>
  <%- include('../partials/navbar') %>

  <main>
    <div class="game-form-container">
      <form action="/auth/change-password" method="post" class="game-form">
        <h2 class="game-form-title">Change Password</h2>

        <input type="password" name="oldPassword" id="oldPassword" placeholder="Old Password" class="game-input" required />
        <input type="password" name="newPassword" id="newPassword" placeholder="New Password" class="game-input" required />
        <input type="password" name="confirmPassword" id="confirmPassword" placeholder="Confirm Password" class="game-input" required />

        <button type="submit" class="game-submit-btn">Change Password</button>

        <% if (typeof error !== 'undefined' && error) { %>
          <p class="game-form-error"><%= error %></p>
        <% } %>
      </form>
    </div>
  </main>
</body>
</html>
