<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Profile | STACKD</title>
  <link rel="stylesheet" href="/styles/style.css" />
  <script src="/js/avatarPreview.js" defer></script>
</head>
<body>
  <%- include('../partials/navbar') %>

  <main>
    <div class="profile-container">
      <form action="/users/profile?_method=PUT" method="post" enctype="multipart/form-data" class="profile-form">

        <label for="avatarUpload" class="avatar-wrapper">
          <img
            id="avatarPreview"
            src="<%= user.avatar ? user.avatar : 'https://res.cloudinary.com/dvhwvkip4/image/upload/v1753900321/Default_xxvq7h.png' %>"
            alt="User Avatar"
          />
          <input type="file" id="avatarUpload" name="avatar" accept="image/*" hidden />
        </label>

        <input name="username" id="username" placeholder="Username" value="<%= user.username %>" class="game-input" required />
        <input name="name" id="name" placeholder="Name" value="<%= user.name %>" class="game-input" />
        <input name="age" id="age" type="number" min="6" placeholder="Age" value="<%= user.age %>" class="game-input" />
        <textarea name="bio" id="bio" placeholder="Tell us about yourself..." class="game-textarea"><%= user.bio %></textarea>

        <button type="submit" class="game-submit-btn">Save Changes</button>

        <a href="/auth/change-password" class="game-label" style="text-align:center;">Change Password</a>

        <% if (typeof error !== 'undefined' && error) { %>
          <p class="game-form-error"><%= error %></p>
        <% } %>
      </form>
    </div>
  </main>
</body>
</html>
