<nav class="sign-navbar">
  <div class="logo-no-text">
    <a href="/">
      <img src="/images/logo.png" alt="STACKD Logo">
    </a>
  </div>

  <div class="nav-links">
    <% if (user) { %>
      <a href="/games/dashboard" <% if (currentPage === 'dashboard') { %>class="active"<% } %>>Dashboard</a>
      <a href="/users/profile" <% if (currentPage === 'profile') { %>class="active"<% } %>>Profile</a>
      <form action="/auth/logout" method="post" style="display:inline;">
        <button type="submit" class="nav-link-button">Logout</button>
      </form>
    <% } else { %>
      <a href="/auth/login" <% if (currentPage === 'login') { %>class="active"<% } %>>Login</a>
      <a href="/auth/sign-up" <% if (currentPage === 'signup') { %>class="active"<% } %>>Sign Up</a>
    <% } %>
  </div>
</nav>